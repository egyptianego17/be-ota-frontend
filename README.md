# OTA Frontend

Admin UI for the OTA service in `be-ota-backend`. Upload firmware bundles, tag device groups, kick off staged rollouts, and watch the device check-in stream as updates spread.

Next.js + TypeScript. Built so a non-developer field engineer can use it — most actions are two clicks deep.
